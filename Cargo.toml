[workspace]
resolver = "2"
members = [
    "src/coordinator",
    "src/pinaivu-protocol",
    "src/aws",
    "src/init",
    "src/system",
    "src/nautilus-enclave",
]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"

[profile.dev]
opt-level = 0
