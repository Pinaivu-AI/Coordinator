//! Nitro Enclave init process. Lands in the initramfs; mounts pseudo-
//! filesystems, loads `nsm.ko`, and starts the coordinator binary on
//! VSOCK. Scaffold.

fn main() {
    eprintln!("pinaivu enclave init (scaffold)");
}
